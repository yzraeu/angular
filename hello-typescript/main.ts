var minhaVar = 'minha variavel';

function minhaFunc(x, y)
{
    return x + y;
}

// ES 6 / 2015 
let num = 2;
const PI = 3.1415;

var numeros = [1, 2, 3];

numeros.map(function(valor){
    return valor * 2;
});

numeros.map(v => v * 2);

class Matematica{
    soma(x ,y){
        return x + y;
    }
}

var n1: any = 'sdfsdf';
n1 = 4;